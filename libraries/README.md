# Simple Web - Libraries

**Container** (*container.class.php*)
  This class is the basis for the dependency injection subsystems.

**Template** (*template.class.php*)
  This class contains the core template engine. 
